import { AnosMembroPipe } from './anos-membro-pipe';

describe('AnosMembroPipe', () => {
  it('create an instance', () => {
    const pipe = new AnosMembroPipe();
    expect(pipe).toBeTruthy();
  });
});
