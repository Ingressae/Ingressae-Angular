import { TempoAtrasPipe } from './tempo-atras-pipe';

describe('TempoAtrasPipe', () => {
  it('create an instance', () => {
    const pipe = new TempoAtrasPipe();
    expect(pipe).toBeTruthy();
  });
});
