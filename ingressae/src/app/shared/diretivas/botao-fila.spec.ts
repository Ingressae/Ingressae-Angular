import { BotaoFila } from './botao-fila';

describe('BotaoFila', () => {
  it('should create an instance', () => {
    const directive = new BotaoFila();
    expect(directive).toBeTruthy();
  });
});
